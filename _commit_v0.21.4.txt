v0.21.4: Desktop polish — changelog markdown, wake word UX, auto-updater fixes

Changelog Dialog:
- Replace naive markdown stripper with proper renderer supporting headers,
  bold, italic, code blocks, inline code, links, lists, and horizontal rules
- Add skeleton loading state (3 placeholder cards) instead of bare spinner
- Add aria-label to refresh button

Wake Word:
- Add Python dependency check (SpeechRecognition + PyAudio) before allowing
  toggle; show warning with install instructions if missing
- Add visual tray icon change (green dot overlay) when wake word is listening
- Add offline STT fallback via PocketSphinx when Google API is unavailable
- Add mic icon indicator to status bar when wake word is actively listening
- Update wake word descriptions in zh, zh-hant, ja to mention voice mode

Connections / MCP OAuth:
- Add error indicator (warning icon) when auth status fetch fails
- Disable OAuth login button when server is disabled

Auto-Updater:
- Fix race condition: set isChecking flag before async call, not after
- Fix isDownloading not reset in all failure paths (add finally block)

OAuth Login Window:
- Add 5-minute timeout to prevent hanging if user abandons login

Branding:
- Update Teams adapter User-Agent headers from "Hermes" to "Zeus"

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
